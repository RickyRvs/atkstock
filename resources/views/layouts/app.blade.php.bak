<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@yield('title', 'Sistem Stok ATK/ARK') — BPS Provinsi Riau</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .sidebar-link.active { @apply bg-blue-700 text-white; }
    </style>
</head>
<body class="bg-gray-100 min-h-screen" x-data="{ sidebarOpen: true }">

{{-- SIDEBAR --}}
<div class="flex h-screen overflow-hidden">
    <aside class="bg-blue-900 text-white flex flex-col transition-all duration-300"
           :class="sidebarOpen ? 'w-64' : 'w-16'">

        {{-- Logo --}}
        <div class="flex items-center gap-3 px-4 py-5 border-b border-blue-800">
            <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/b/b6/Lambang_BPS.svg/120px-Lambang_BPS.svg.png"
                 alt="BPS" class="w-8 h-8 flex-shrink-0">
            <div x-show="sidebarOpen" class="overflow-hidden">
                <p class="text-xs font-bold leading-tight">Sistem Stok</p>
                <p class="text-xs text-blue-300 leading-tight">ATK / ARK · Riau</p>
            </div>
        </div>

        {{-- Nav --}}
        <nav class="flex-1 py-4 space-y-1 overflow-y-auto">
            @php
                $navItems = [
                    ['route' => 'dashboard',          'icon' => '📊', 'label' => 'Dashboard'],
                    ['route' => 'barang.index',       'icon' => '📦', 'label' => 'Master Barang'],
                    ['route' => 'kategori.index',     'icon' => '🗂️',  'label' => 'Kategori'],
                    ['route' => 'stok-awal.index',    'icon' => '🗃️',  'label' => 'Stok Awal'],
                    ['route' => 'transaksi.index',    'icon' => '🔄', 'label' => 'Transaksi'],
                    ['route' => 'transaksi.masuk.create',  'icon' => '⬇️',  'label' => 'Barang Masuk'],
                    ['route' => 'transaksi.keluar.create', 'icon' => '⬆️',  'label' => 'Barang Keluar'],
                    ['route' => 'laporan.kartu-persediaan','icon' => '📋', 'label' => 'Kartu Persediaan'],
                    ['route' => 'laporan.bulanan',    'icon' => '📅', 'label' => 'Laporan Bulanan'],
                    ['route' => 'laporan.tahunan',    'icon' => '📆', 'label' => 'Rekap Tahunan'],
                ];
            @endphp

            @foreach($navItems as $item)
                <a href="{{ route($item['route']) }}"
                   class="flex items-center gap-3 px-4 py-2.5 text-sm hover:bg-blue-700 transition-colors
                          {{ request()->routeIs(rtrim($item['route'], '.index') . '*') ? 'bg-blue-700 font-semibold' : 'text-blue-100' }}">
                    <span class="text-base flex-shrink-0">{{ $item['icon'] }}</span>
                    <span x-show="sidebarOpen" class="truncate">{{ $item['label'] }}</span>
                </a>
            @endforeach

            @if(auth()->user()->isAdmin())
                <div class="px-4 pt-4 pb-1" x-show="sidebarOpen">
                    <p class="text-xs text-blue-400 uppercase tracking-widest">Admin</p>
                </div>
                <a href="{{ route('users.index') }}"
                   class="flex items-center gap-3 px-4 py-2.5 text-sm hover:bg-blue-700 transition-colors
                          {{ request()->routeIs('users*') ? 'bg-blue-700 font-semibold' : 'text-blue-100' }}">
                    <span class="text-base flex-shrink-0">👥</span>
                    <span x-show="sidebarOpen">Manajemen User</span>
                </a>
            @endif
        </nav>

        {{-- User --}}
        <div class="border-t border-blue-800 px-4 py-3 flex items-center gap-3">
            <div class="w-8 h-8 rounded-full bg-blue-600 flex items-center justify-center text-sm font-bold flex-shrink-0">
                {{ strtoupper(substr(auth()->user()->name, 0, 1)) }}
            </div>
            <div x-show="sidebarOpen" class="flex-1 overflow-hidden">
                <p class="text-xs font-semibold truncate">{{ auth()->user()->name }}</p>
                <p class="text-xs text-blue-300 capitalize">{{ auth()->user()->role }}</p>
            </div>
            <form method="POST" action="{{ route('logout') }}" x-show="sidebarOpen">
                @csrf
                <button type="submit" class="text-blue-300 hover:text-white text-xs">Keluar</button>
            </form>
        </div>
    </aside>

    {{-- MAIN CONTENT --}}
    <div class="flex-1 flex flex-col overflow-hidden">
        {{-- Topbar --}}
        <header class="bg-white shadow-sm px-6 py-3 flex items-center justify-between flex-shrink-0">
            <div class="flex items-center gap-4">
                <button @click="sidebarOpen = !sidebarOpen" class="text-gray-500 hover:text-gray-700">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                    </svg>
                </button>
                <h1 class="text-base font-semibold text-gray-700">@yield('title', 'Dashboard')</h1>
            </div>
            <span class="text-xs text-gray-400">{{ now()->isoFormat('dddd, D MMMM Y') }}</span>
        </header>

        {{-- Flash Messages --}}
        <div class="px-6 pt-4">
            @if(session('success'))
                <div class="bg-green-50 border border-green-200 text-green-800 text-sm px-4 py-3 rounded-lg mb-2 flex justify-between"
                     x-data x-init="setTimeout(() => $el.remove(), 4000)">
                    <span>✅ {{ session('success') }}</span>
                    <button @click="$el.remove()">✕</button>
                </div>
            @endif
            @if(session('error'))
                <div class="bg-red-50 border border-red-200 text-red-800 text-sm px-4 py-3 rounded-lg mb-2 flex justify-between"
                     x-data x-init="setTimeout(() => $el.remove(), 5000)">
                    <span>⚠️ {{ session('error') }}</span>
                    <button @click="$el.remove()">✕</button>
                </div>
            @endif
        </div>

        {{-- Page Content --}}
        <main class="flex-1 overflow-y-auto px-6 py-4">
            @yield('content')
        </main>
    </div>
</div>

</body>
</html>
